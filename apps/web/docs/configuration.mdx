---
title: Configuration
description: Environment variables and defaults.
order: 5
---

ai-cli is configured entirely through environment variables. There is no config file.

## Environment variables

<Properties>
  <Property name="AI_CLI_GATEWAY" type="vercel | cloudflare">
    Gateway backend. Defaults to `vercel`.
  </Property>
  <Property name="AI_GATEWAY_API_KEY" type="string">
    Vercel AI Gateway API key. Provides access to all supported models.
  </Property>
  <Property name="CLOUDFLARE_ACCOUNT_ID" type="string">
    Cloudflare account ID. Required when `AI_CLI_GATEWAY=cloudflare`.
  </Property>
  <Property name="CLOUDFLARE_AI_GATEWAY_ID" type="string">
    Cloudflare AI Gateway name. Defaults to `default`.
  </Property>
  <Property name="CLOUDFLARE_AI_GATEWAY_TOKEN" type="string">
    Preferred Cloudflare authentication token. Required in Cloudflare mode and
    must grant AI Gateway Run.
  </Property>
  <Property name="CLOUDFLARE_API_TOKEN" type="string">
    Fallback Cloudflare token when `CLOUDFLARE_AI_GATEWAY_TOKEN` is unset. It
    must grant AI Gateway Run.
  </Property>
  <Property name="AI_CLI_TEXT_MODEL" type="string">
    Default text model. Overrides `openai/gpt-5.5`.
  </Property>
  <Property name="AI_CLI_IMAGE_MODEL" type="string">
    Default image model. Overrides `openai/gpt-image-2`.
  </Property>
  <Property name="AI_CLI_VIDEO_MODEL" type="string">
    Default video model. Overrides `bytedance/seedance-2.0`.
  </Property>
  <Property name="AI_CLI_SPEECH_MODEL" type="string">
    Default speech model. Overrides `openai/tts-1`.
  </Property>
  <Property name="AI_CLI_TRANSCRIPTION_MODEL" type="string">
    Default transcription model. Overrides `openai/whisper-1`.
  </Property>
  <Property name="AI_CLI_OUTPUT_DIR" type="path">
    Default output directory for generated files.
  </Property>
  <Property name="AI_CLI_PREVIEW" type="0 | 1">
    `1` forces inline preview on, `0` disables it.
  </Property>
  <Property name="NO_COLOR" type="any">
    Disable ANSI color output.
  </Property>
  <Property name="FORCE_COLOR" type="any">
    Force color output even when not a TTY.
  </Property>
</Properties>

## Precedence

- `-m` flag always takes priority over `AI_CLI_*_MODEL` env vars.
- `-o` flag always takes priority over `AI_CLI_OUTPUT_DIR`.
- `--no-preview` takes priority over `AI_CLI_PREVIEW`.
- `CLOUDFLARE_AI_GATEWAY_TOKEN` takes priority over `CLOUDFLARE_API_TOKEN`.

## Example shell profile

```bash
export AI_GATEWAY_API_KEY="gw_..."
export AI_CLI_TEXT_MODEL="anthropic/claude-sonnet-4"
export AI_CLI_IMAGE_MODEL="bfl/flux-2-pro"
export AI_CLI_SPEECH_MODEL="openai/tts-1"
export AI_CLI_OUTPUT_DIR="$HOME/ai-output"
```

Cloudflare example:

```bash
export AI_CLI_GATEWAY="cloudflare"
export CLOUDFLARE_ACCOUNT_ID="your-account-id"
export CLOUDFLARE_AI_GATEWAY_ID="default"
export CLOUDFLARE_AI_GATEWAY_TOKEN="your-ai-gateway-run-token"
```

Cloudflare mode requires an [authenticated gateway](https://developers.cloudflare.com/ai-gateway/configuration/authentication/). Store OpenAI, Google AI Studio, OpenRouter, Replicate, and Fal credentials in the gateway's **Provider Keys** page with the `default` alias. ai-cli does not read or forward local provider keys or provider authorization headers in this mode.
